# Markdown 文档处理能力全景图

> 按用户上传 md 文档后的所有可能场景，逐一评估自然语言理解与平台执行能力。

---

## 图例

| 符号 | 含义 |
|------|------|
| ✅ | 已实现，能正确响应 |
| ⚠️ | 部分实现，有限制 |
| ❌ | 未实现，需要开发 |

---

## 场景一览

### 一、读懂文档

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 1 | "帮我看看这份md写了什么" | ✅ 能 | ⚠️ `POST /md/parse` 返回结构JSON，但不是自然语言摘要 | 缺"用大白话总结md内容" |
| 2 | "这份文档有几个章节？标题是什么？" | ✅ 能 | ✅ `POST /md/parse` 返回 toc 和 headings | |
| 3 | "里面有几个表格？" | ✅ 能 | ✅ `POST /md/parse` 返回 tables 数量 | |
| 4 | "这篇文档主要讲了什么？给我一个摘要" | ✅ 能 | ⚠️ 没有 /md/summarize 端点 | 可走 `/md/rewrite` 变通 |

### 二、搜索与问答

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 5 | "文档里有没有提到'函证'相关内容？" | ✅ 能 | ⚠️ RAG可以检索，但没有"单文档内搜索"的端点 | 可走 `/rag/search` 但不够精确 |
| 6 | "第三章讲了什么？帮我提取出来" | ✅ 能 | ❌ 没有按章节提取的端点 | `parse_md`有结构，但没暴露提取API |
| 7 | "找出文档中所有包含数字的段落" | ✅ 能 | ❌ | |

### 三、模板填充

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 8 | "把这几个数字填进去：金额125万，日期2026年7月" | ✅ 能 | ✅ `POST /md/fill` 做 {{ }} 替换 | 需要模板里预先有占位符 |
| 9 | "这是审计报告模板，帮我把审定数填到第三章" | ✅ 能 | ✅ `POST /md/intelligent-fill` | LLM理解模板结构后智能填充 |
| 10 | "用这份md模板，结合刚才跑出来的差异明细，生成一份完整的报告" | ✅ 能 | ✅ 先RAG检索差异数据→再 intelligent-fill | 跨步骤，需要前端编排 |

### 四、生成文档

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 11 | "帮我生成一份银行流水核对的审计说明" | ✅ 能 | ✅ `POST /md/generate` | LLM动态生成结构 |
| 12 | "根据这些数据，生成一份带表格的函证管理报告" | ✅ 能 | ✅ | LLM会生成含表格的md |
| 13 | "给我一份markdown格式的抽样报告，要有标题、方法说明、样本清单" | ✅ 能 | ✅ | LLM按用户要求生成 |

### 五、改写与润色

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 14 | "帮我把这份文档改得更正式一点" | ✅ 能 | ✅ `POST /md/rewrite` | instruction="改得更正式" |
| 15 | "把审计结论那一段改得更专业，用中注协标准措辞" | ✅ 能 | ✅ | LLM+RAG检索准则措辞→改写 |
| 16 | "数字全部改成千分位格式" | ✅ 能 | ✅ | |
| 17 | "把'我们觉得'改成'我们认为'" | ✅ 能 | ✅ | 精确替换可走md/rewrite |

### 六、格式转换

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 18 | "把这份md转成Word" | ✅ 能 | ✅ `POST /md/convert` target=docx | |
| 19 | "把这份md转成HTML，我要放网页上" | ✅ 能 | ✅ `POST /md/convert` target=html | |
| 20 | "生成完的报告，直接出Word版本" | ✅ 能 | ✅ 用 `/md/intelligent-fill` out_format=docx | |

### 七、多文档操作

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 21 | "把这两份md合并成一份" | ✅ 能 | ❌ | 没有合并端点 |
| 22 | "对比这两份报告，找出数字不一样的地方" | ✅ 能 | ❌ | 没有diff端点，但同源勾稽引擎有基础 |
| 23 | "把五个医院的报告汇总成一份总报告" | ✅ 能 | ❌ | |

### 八、格式规范化

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 24 | "把这篇md的格式搞得规范一点，标题用#，表格对齐" | ✅ 能 | ⚠️ 可走 `/md/rewrite` | 但rewrite可能改内容，不够精确 |
| 25 | "所有标题统一用二级标题##" | ✅ 能 | ❌ | 缺格式规则化端点 |
| 26 | "数字全部保留两位小数" | ✅ 能 | ⚠️ 可走 `/md/rewrite` | |

### 九、拆分与提取

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 27 | "把第三章单独提取出来" | ✅ 能 | ❌ | parse_md有结构，没暴露提取API |
| 28 | "把这份长文档拆成三份：审计目标、执行过程、审计结论" | ✅ 能 | ❌ | |
| 29 | "把所有的表格单独导出来" | ✅ 能 | ❌ | parse_md能识别表格，没单独导出 |

### 十、校验与检查

| # | 用户可能说的自然语言 | LLM能理解吗 | 平台能做吗 | 备注 |
|---|-------------------|------------|-----------|------|
| 30 | "检查这篇报告有没有缺审计结论" | ✅ 能 | ❌ | 没有内容校验端点 |
| 31 | "报告里的数字勾稽对不对" | ✅ 能 | ❌ | 同源勾稽引擎有基础，未接入md |
| 32 | "这篇文档的格式符合规范吗" | ✅ 能 | ❌ | |

---

## 统计

| 状态 | 数量 | 占比 |
|------|------|------|
| ✅ 已实现 | 16 | 50% |
| ⚠️ 部分实现 | 4 | 13% |
| ❌ 未实现 | 12 | 37% |

---

## 优先补充清单

| 优先级 | 端点 | 覆盖场景 | 工作量 |
|--------|------|---------|--------|
| P0 | `/md/summarize` | 1, 4 | 30行 |
| P0 | `/md/extract` | 6, 27, 28, 29 | 50行 |
| P1 | `/md/merge` | 21, 23 | 20行 |
| P1 | `/md/diff` | 22, 31 | 40行 |
| P1 | `/md/validate` | 30, 32 | 30行 |
| P2 | `/md/search` | 5, 7 | 20行 |
